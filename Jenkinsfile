pipeline {
    agent any

    stages {
        
        // checkout

        stage("build") {
            steps {
                echo 'building the application...'
                // sh 'npm install'
                // sh 'npm build'
            }
        }

        // test
        // deploy
        // cleanup
    }
}